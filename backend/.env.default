# Database Configuration - Default PostgreSQL Setup
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/worksense_db

# Alternative database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=worksense_db
DB_USER=postgres
DB_PASSWORD=postgres

# Server Configuration
PORT=4001
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Logging
LOG_LEVEL=info
